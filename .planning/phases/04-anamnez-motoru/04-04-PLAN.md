---
phase: 04-anamnez-motoru
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/app/api/sessions/[id]/anamnesis/route.ts
  - src/app/api/sessions/[id]/complete/route.ts
  - src/components/patients/StartSessionButton.tsx
  - src/components/sessions/TemplatePicker.tsx
  - src/components/sessions/AnamnesisForm.tsx
  - src/components/sessions/ConsentGate.tsx
  - src/components/sessions/SessionWorkspace.tsx
  - src/lib/sessions/types.ts
autonomous: true
requirements: [TPLT-05, ANAM-01, ANAM-02, ANAM-03, ANAM-04, ANAM-05, ANAM-06]
must_haves:
  truths:
    - "Dentist selects a published template before starting a session; that version's questions load"
    - "After recording stops, AI fills the form and each answer shows a confidence indicator"
    - "Dentist can edit any AI-filled field, and edits mark the field as human-edited"
    - "Unanswered required questions appear as alerts; clicking one focuses its field"
    - "The save button is disabled until both KVKK and informed-consent checkboxes are checked"
    - "Saving persists answers + consent and marks the session completed"
  artifacts:
    - path: "src/app/api/sessions/[id]/anamnesis/route.ts"
      provides: "POST run AI mapping + persist answers; GET load answers"
      exports: ["GET", "POST"]
    - path: "src/app/api/sessions/[id]/complete/route.ts"
      provides: "POST finalize — requires both consent flags"
      exports: ["POST"]
    - path: "src/components/sessions/AnamnesisForm.tsx"
      provides: "Per-question fields with confidence badges, edit, missing-field alerts"
      min_lines: 120
    - path: "src/components/sessions/ConsentGate.tsx"
      provides: "KVKK + informed consent checkboxes gating save"
      min_lines: 30
  key_links:
    - from: "src/app/api/sessions/[id]/anamnesis/route.ts"
      to: "mapTranscriptToAnswers"
      via: "import from @/lib/openai/anamnesis"
      pattern: "mapTranscriptToAnswers"
    - from: "src/components/sessions/ConsentGate.tsx"
      to: "/api/sessions/[id]/complete"
      via: "save disabled until both checkboxes; POST on save"
      pattern: "fetch.*complete"
    - from: "src/components/sessions/AnamnesisForm.tsx"
      to: "missing-field alert click"
      via: "focuses field by question_id"
      pattern: "focus"
---

<objective>
Wire the anamnesis experience end-to-end: pick a published template at session start, run GPT-4o on the transcript after recording, render the filled form with confidence indicators and inline editing, surface missing-question alerts with click-to-focus, and gate saving behind KVKK + informed-consent checkboxes.

Purpose: TPLT-05 + ANAM-01..06 — the dentist-facing half of Phase 4. Consumes the schema/AI engine (04-03) and template versions (04-01/02).
Output: 2 API routes + 4 components + StartSessionButton/SessionWorkspace/types updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-anamnez-motoru/04-01-SUMMARY.md
@.planning/phases/04-anamnez-motoru/04-03-SUMMARY.md

<interfaces>
<!-- Use directly. Do NOT explore. -->

From 04-01 (src/lib/templates/types.ts, src/lib/anamnesis/types.ts):
TemplateVersionRow { id, template_id, version, name, department, questions: SnapshotQuestion[] },
SnapshotQuestion { id, prompt, question_type, options, position, required },
AnamnesisAnswerDTO { question_id, prompt, question_type, answer_value, confidence, edited_by_human },
AnswerValue (boolean|string|string[]|number|null), MissingFieldAlert { question_id, prompt }.

From 04-03 (src/lib/openai/anamnesis.ts): `mapTranscriptToAnswers(transcript, questions) → AiMappingResult { answers: AiMappedAnswer[], corrected_transcript? }`, AnamnesisMappingError.
From 04-03 (src/lib/anamnesis/mapper.ts): `buildMissingAlerts(answers, questions) → MissingFieldAlert[]`.

DB (from 04-01 migration): sessions has template_version_id, kvkk_consent, informed_consent.
anamnesis_answers UNIQUE(session_id, question_id). sessions consent CHECK blocks status='completed'
unless kvkk_consent AND informed_consent.

Existing session page (src/app/patients/[id]/sessions/[sessionId]/page.tsx) server-fetches the
session row + transcript and renders <SessionWorkspace>. SessionWorkspace shows the recorder while
draft, and a static LiveTranscript replay when recorder_state==='completed' or status==='completed'
— THIS replay branch is where the AnamnesisForm mounts.

API conventions: src/app/api/sessions/[id]/transcript/route.ts (auth, ownership .eq('user_id'),
RouteContext { params: Promise<{ id: string }> }). Turkish error strings. supabaseAdmin.

StartSessionButton (src/components/patients/StartSessionButton.tsx) currently: picks codec, POSTs
/api/sessions with { patient_id, form_type, audio_format }, pushes to the session page.

UI primitives: src/components/ui/{button,card,input,label,badge,checkbox? (none — build inline),
dialog,select(@base-ui),table,sonner}. toast from 'sonner'. lucide-react icons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template picker at session start (TPLT-05)</name>
  <read_first>
    - src/components/patients/StartSessionButton.tsx (current start flow to extend)
    - src/app/api/sessions/route.ts (POST body + how session is created; needs template_version_id)
    - src/app/api/templates/route.ts (list endpoint to fetch published templates)
    - src/lib/sessions/types.ts (CreateSessionBody to extend)
    - src/lib/templates/types.ts (TemplateListItem)
  </read_first>
  <files>src/components/sessions/TemplatePicker.tsx, src/components/patients/StartSessionButton.tsx, src/lib/sessions/types.ts, src/app/api/sessions/route.ts</files>
  <action>
1. `src/lib/sessions/types.ts`: extend `CreateSessionBody` with `template_version_id?: string`. Add `export interface SessionTemplateInfo { template_version_id: string; version: number; questions: import('@/lib/templates/types').SnapshotQuestion[] }`.

2. `src/app/api/sessions/route.ts` POST: accept optional `template_version_id`. If present, verify it belongs to the user (`template_versions` row where id + user_id) → 404 'Şablon sürümü bulunamadı.' if not. Insert it into the new `sessions.template_version_id` column. When a template_version_id is given, set `form_type = 'anamnez'`. (Leave the existing codec/validation flow untouched.)

3. `src/components/sessions/TemplatePicker.tsx` ('use client'): a dialog/select that GETs `/api/templates`, filters to `current_version >= 1` (published), and lets the dentist pick one. For the picked template, it needs the latest published `template_version_id` — add to /api/templates GET (in 04-02 it returns current_version; here also fetch the version id). To keep this plan self-contained: TemplatePicker GETs `/api/templates`, and on selection GETs `/api/templates/{id}` then resolves the version id by querying a new lightweight endpoint OR include `latest_version_id` in the TemplateListItem. IMPLEMENT: add `latest_version_id: string | null` to the GET /api/templates response (update the select to left-join template_versions on (template_id, version=current_version)). TemplatePicker returns the chosen `latest_version_id`.

4. `src/components/patients/StartSessionButton.tsx`: before POSTing /api/sessions, render `<TemplatePicker onSelect={(versionId) => ...}>`. Pass the chosen `template_version_id` into the CreateSessionBody. If the dentist starts without picking, allow it (form_type stays 'genel', no AI form) — but default the button label to open the picker first. Keep the existing codec negotiation and redirect.
  </action>
  <verify>
    <automated>test -f src/components/sessions/TemplatePicker.tsx && grep -q "template_version_id" src/lib/sessions/types.ts && grep -q "template_version_id" src/app/api/sessions/route.ts && grep -q "latest_version_id" src/app/api/templates/route.ts && grep -q "TemplatePicker" src/components/patients/StartSessionButton.tsx && npx tsc --noEmit -p . 2>&1 | grep -E "TemplatePicker|StartSession" | head; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - CreateSessionBody has optional template_version_id; POST /api/sessions validates + stores it and sets form_type='anamnez' when present.
    - GET /api/templates returns latest_version_id (published version id or null).
    - TemplatePicker lists only published templates and returns a version id.
    - StartSessionButton wires the picked version into session creation.
  </acceptance_criteria>
  <done>Dentist picks a published template; the session is bound to that immutable version.</done>
</task>

<task type="auto">
  <name>Task 2: Anamnesis API — run AI mapping, persist answers, complete with consent</name>
  <read_first>
    - src/app/api/sessions/[id]/transcript/route.ts (ownership + transcript fetch pattern)
    - src/lib/openai/anamnesis.ts (mapTranscriptToAnswers — 04-03)
    - src/lib/anamnesis/mapper.ts (buildMissingAlerts — 04-03)
    - src/lib/anamnesis/types.ts (AnamnesisAnswerDTO, AnswerValue)
    - supabase/migrations/20260611000001_create_anamnesis_tables.sql (anamnesis_answers + sessions consent columns)
  </read_first>
  <files>src/app/api/sessions/[id]/anamnesis/route.ts, src/app/api/sessions/[id]/complete/route.ts</files>
  <action>
`src/app/api/sessions/[id]/anamnesis/route.ts` (RouteContext `{ params: Promise<{ id:string }> }`, `runtime='nodejs'`, `maxDuration=60`):
- `POST` (run mapping): auth + ownership. Load the session row; 422 if `template_version_id` is null ('Bu seansa şablon atanmamış.'). Load the `template_versions` row → its `questions` (SnapshotQuestion[]). Concatenate `transcript_segments.content` (ordered by sequence) into one transcript string. Call `mapTranscriptToAnswers(transcript, questions)`. For each question, upsert an `anamnesis_answers` row `{ user_id, session_id, question_id, prompt, question_type, answer_value, confidence, edited_by_human:false }` — use `.upsert(..., { onConflict: 'session_id,question_id' })`. Compute `buildMissingAlerts(answers, questions)`. Return `{ answers: AnamnesisAnswerDTO[], missing: MissingFieldAlert[], corrected_transcript }`. Catch AnamnesisMappingError → 502 'AI form doldurma başarısız.'.
- `GET` (load saved answers): auth + ownership. Return existing `anamnesis_answers` for the session as `AnamnesisAnswerDTO[]` ordered to match question position (join order via the template_versions.questions positions). Used on reload.
- `PATCH` (manual edit, ANAM-03): body `{ question_id, answer_value }`. Update the one answer row, set `edited_by_human = true`, `confidence = null` (human-set = no AI confidence). 404 if answer/session not owned.

`src/app/api/sessions/[id]/complete/route.ts`:
- `POST` (finalize, ANAM-06): body `{ kvkk_consent: boolean, informed_consent: boolean }`. auth + ownership. 422 'KVKK ve onam onayı gereklidir.' unless BOTH are true. Update session: `kvkk_consent`, `informed_consent`, `status='completed'`, `completed_at=now()`, `recorder_state='completed'`. The DB CHECK from 04-01 is the backstop — surface a 409 if the update is rejected by the constraint. Return the updated session summary 200.
  </action>
  <verify>
    <automated>test -f "src/app/api/sessions/[id]/anamnesis/route.ts" && test -f "src/app/api/sessions/[id]/complete/route.ts" && grep -q "mapTranscriptToAnswers" "src/app/api/sessions/[id]/anamnesis/route.ts" && grep -q "buildMissingAlerts" "src/app/api/sessions/[id]/anamnesis/route.ts" && grep -q "onConflict" "src/app/api/sessions/[id]/anamnesis/route.ts" && grep -qE "kvkk_consent.*informed_consent|informed_consent.*kvkk_consent" "src/app/api/sessions/[id]/complete/route.ts" && grep -q "status: 'completed'" "src/app/api/sessions/[id]/complete/route.ts" && npx tsc --noEmit -p . 2>&1 | grep -E "anamnesis/route|complete/route" | head; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - POST /anamnesis runs mapTranscriptToAnswers and upserts answers keyed by (session_id, question_id).
    - Response includes missing-field alerts and corrected_transcript.
    - PATCH sets edited_by_human=true and confidence=null.
    - POST /complete rejects with 422 unless both consent flags true; sets status='completed'.
  </acceptance_criteria>
  <done>API can fill the form from the transcript, accept manual edits, and finalize only with consent.</done>
</task>

<task type="auto">
  <name>Task 3: Anamnesis form UI + consent gate + missing alerts</name>
  <read_first>
    - src/components/sessions/SessionWorkspace.tsx (completed/replay branch where the form mounts)
    - src/components/sessions/LiveTranscript.tsx (styling reference)
    - src/app/patients/[id]/sessions/[sessionId]/page.tsx (what props SessionWorkspace receives)
    - src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/input.tsx (primitives)
    - src/lib/anamnesis/types.ts (AnamnesisAnswerDTO, AnswerValue, MissingFieldAlert)
  </read_first>
  <files>src/components/sessions/AnamnesisForm.tsx, src/components/sessions/ConsentGate.tsx, src/components/sessions/SessionWorkspace.tsx, src/app/patients/[id]/sessions/[sessionId]/page.tsx</files>
  <action>
`src/components/sessions/ConsentGate.tsx` ('use client', min 30 lines): two labeled checkboxes — KVKK veri işleme onayı + Aydınlatılmış onam (informed consent). A "Seansı Kaydet" button DISABLED until both checked. On click, POST `/api/sessions/{id}/complete` with `{ kvkk_consent:true, informed_consent:true }`; on success call `onSaved()` (router push to patient profile); toast.error on failure. Build the checkbox inline (no ui/checkbox component exists) with a native `<input type="checkbox">` styled with Tailwind, bound to local state.

`src/components/sessions/AnamnesisForm.tsx` ('use client', min 120 lines):
- Props: `{ sessionId, patientId, questions: SnapshotQuestion[], initialAnswers: AnamnesisAnswerDTO[], initialMissing: MissingFieldAlert[] }`.
- On mount, if `initialAnswers` is empty, POST `/api/sessions/{id}/anamnesis` to run the AI fill; show a "AI form dolduruyor…" spinner; populate state from the response. Otherwise seed from initialAnswers.
- Render one field per question (ordered by position) keyed by question_id, each with `ref` registered into a `Record<string, HTMLElement>` for focus (ANAM-05). Field input depends on question_type: yes_no → toggle/segmented Evet/Hayır; text → Input; numeric → number Input; multi_select → checkbox group from question.options.
- Confidence indicator per AI-filled field (ANAM-02): a `Badge` colored by confidence (≥0.8 green 'Yüksek', 0.5–0.8 amber 'Orta', <0.5 red 'Düşük'); hide the badge when `edited_by_human` (show a "Düzenlendi" badge instead).
- Editing any field (ANAM-03): debounced PATCH `/api/sessions/{id}/anamnesis` `{ question_id, answer_value }`; mark the field edited locally and swap its badge to "Düzenlendi".
- Missing-field alerts panel (ANAM-04): list `missing` entries; each is a button that on click calls `.focus()` / `scrollIntoView()` on the corresponding field ref (ANAM-05). Recompute the missing list client-side as the user fills fields (a required question with a value drops off the list).
- Render `<ConsentGate sessionId={sessionId} onSaved={() => router.push(`/patients/${patientId}`)} />` at the bottom.

`src/components/sessions/SessionWorkspace.tsx`: in the `completed`/replay branch (currently just LiveTranscript), ALSO render `<AnamnesisForm .../>` when the session has a `templateVersion` with questions. Add new props to SessionWorkspace: `templateVersionQuestions: SnapshotQuestion[] | null`, `initialAnswers: AnamnesisAnswerDTO[]`, `initialMissing: MissingFieldAlert[]`. If `templateVersionQuestions` is null, keep current behavior (transcript-only). Note the recorder branch (draft) is unchanged — the form appears once recording is stopped/completed.

`src/app/patients/[id]/sessions/[sessionId]/page.tsx`: extend the server fetch — also select `template_version_id` from the session; if present, fetch the `template_versions.questions` and the existing `anamnesis_answers` (as AnamnesisAnswerDTO[]); compute initialMissing via `buildMissingAlerts`. Pass all three new props into SessionWorkspace. (Import buildMissingAlerts from @/lib/anamnesis/mapper.)
  </action>
  <verify>
    <automated>test -f src/components/sessions/AnamnesisForm.tsx && test -f src/components/sessions/ConsentGate.tsx && grep -q "disabled" src/components/sessions/ConsentGate.tsx && grep -qE "kvkk|informed" src/components/sessions/ConsentGate.tsx && grep -q "complete" src/components/sessions/ConsentGate.tsx && grep -qiE "confidence|Yüksek|Orta|Düşük" src/components/sessions/AnamnesisForm.tsx && grep -q "focus" src/components/sessions/AnamnesisForm.tsx && grep -q "AnamnesisForm" src/components/sessions/SessionWorkspace.tsx && grep -q "template_version_id" "src/app/patients/[id]/sessions/[sessionId]/page.tsx" && npx tsc --noEmit -p . 2>&1 | grep -E "AnamnesisForm|ConsentGate|SessionWorkspace" | head; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - AnamnesisForm renders a field per question with a confidence badge (3-tier) and runs AI fill on mount when empty.
    - Editing a field PATCHes and swaps the badge to "Düzenlendi".
    - Missing-field alert click focuses the matching field (ANAM-05).
    - ConsentGate save button is disabled until both checkboxes checked; POSTs /complete.
    - SessionWorkspace mounts AnamnesisForm in the completed/replay branch when a template version exists.
    - tsc clean for all four files.
  </acceptance_criteria>
  <done>Full dentist anamnesis flow: AI-filled form with confidence, manual edit, missing alerts with focus, consent-gated save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/sessions/[id]/{anamnesis,complete} | Dentist input + AI-derived answers cross into KVKK health-data writes |
| save action → session completion | Consent must be enforced server-side, not just by a disabled button |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | client bypasses disabled save button, POSTs /complete without consent | mitigate | /complete route 422 unless both flags true + DB CHECK `sessions_consent_required_when_completed` as backstop |
| T-04-12 | Spoofing | editing another user's session answers | mitigate | every route checks `auth()` + `.eq('user_id', userId)` ownership on session before any write |
| T-04-13 | Repudiation | unclear which fields were AI vs human | mitigate | edited_by_human flag persisted on PATCH; UI shows "Düzenlendi" badge |
| T-04-14 | Information Disclosure | answers (health data) exposed cross-user | mitigate | RLS user_isolation on anamnesis_answers + app-layer user_id filter |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Save blocked until both consent checkboxes checked (button disabled) AND server-rejected without consent.
- AnamnesisForm shows confidence badges and missing-field alerts that focus fields.
</verification>

<success_criteria>
- TPLT-05: template selected at session start; correct question set loads.
- ANAM-01/02: AI fills form with per-field confidence indicators.
- ANAM-03: any field manually editable (marks human-edited).
- ANAM-04/05: missing-question alerts; click focuses the field.
- ANAM-06: session not saveable without KVKK + informed consent (UI + API + DB).
</success_criteria>

<output>
After completion, create `.planning/phases/04-anamnez-motoru/04-04-SUMMARY.md`
</output>
