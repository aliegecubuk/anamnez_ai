---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/globals.css
  - components.json
  - vercel.json
  - .env.example
  - .env.local
  - vitest.config.ts
  - playwright.config.ts
autonomous: false
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "Next.js 15.2.4 project exists with TypeScript, Tailwind v4, ESLint, App Router, src-dir layout"
    - "shadcn/ui is initialized — components.json committed"
    - "Tailwind v4 @theme block with design tokens from UI-SPEC is in globals.css"
    - "vercel.json pins all functions to fra1 region"
    - ".env.example documents all required environment variables"
    - "Vitest and Playwright configs are in place for future test phases"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with exact next@15.2.4"
      contains: '"next": "15.2.4"'
    - path: "components.json"
      provides: "shadcn/ui config with Tailwind v4 settings"
      contains: '"css": "src/app/globals.css"'
    - path: "vercel.json"
      provides: "Vercel region and function config"
      contains: '"fra1"'
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens"
      contains: "@theme"
    - path: ".env.example"
      provides: "Documentation of all required env vars"
  key_links:
    - from: "src/app/layout.tsx"
      to: "ClerkProvider"
      via: "import from @clerk/nextjs"
      pattern: "ClerkProvider"
    - from: "src/app/globals.css"
      to: "@theme block"
      via: "Tailwind v4 CSS-native config"
      pattern: "@theme"
---

<objective>
Scaffold the AnamnezAl Next.js 15.2.4 project with all foundational tooling. Initialize shadcn/ui with Tailwind v4, install all Phase 1 dependencies, configure vercel.json for fra1 data residency, and set up test infrastructure stubs. This plan establishes the baseline every subsequent plan builds on.

Purpose: Every Phase 1 plan depends on having a correctly configured Next.js project. Wrong Next.js version = CVE exposure. Missing shadcn init = components cannot be added in later waves. Missing fra1 pin = KVKK data residency violation.

Output: Runnable Next.js 15.2.4 project with shadcn initialized, Tailwind v4 design tokens, vercel.json, .env.example, and test config files.
</objective>

<execution_context>
@C:/Users/Gaming/Desktop/AnamnezAl/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Gaming/Desktop/AnamnezAl/.planning/PROJECT.md
@C:/Users/Gaming/Desktop/AnamnezAl/.planning/ROADMAP.md
@C:/Users/Gaming/Desktop/AnamnezAl/.planning/phases/01-temel-altyapi/01-CONTEXT.md
@C:/Users/Gaming/Desktop/AnamnezAl/.planning/phases/01-temel-altyapi/01-RESEARCH.md
@C:/Users/Gaming/Desktop/AnamnezAl/.planning/phases/01-temel-altyapi/01-UI-SPEC.md

<interfaces>
<!-- Greenfield project — no existing interfaces. All contracts established here. -->

Environment variables required (from RESEARCH.md §7):
```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
CLERK_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
NEXT_PUBLIC_ROOT_DOMAIN=localhost
```

Design tokens from UI-SPEC:
- Colors: slate-50 (#F8FAFC) background, white surfaces, blue-600 (#2563EB) accent, red-600 (#DC2626) destructive
- Text: slate-900 (#0F172A) primary, slate-600 (#475569) secondary, slate-400 (#94A3B8) disabled
- Font: Inter via next/font/google
- Spacing: xs=4px, sm=8px, md=16px, lg=24px, xl=32px, 2xl=48px, 3xl=64px

shadcn components needed in Phase 1 (install all now):
button, input, label, form, card, dialog, table, badge, sonner, dropdown-menu, alert, separator
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provision External Services</name>
  <what-built>Nothing built yet — this step verifies external prerequisites exist before code is written.</what-built>
  <how-to-verify>
    Before the executor writes a single line of code, confirm all three services are provisioned:

    1. **Clerk account:** Go to clerk.com → Create account → Create application → Enable Organizations feature in dashboard → Note: Publishable Key and Secret Key from API Keys page.

    2. **Supabase project (CRITICAL — region cannot change after creation):**
       - Go to supabase.com → New project
       - Region: **Europe (Frankfurt) eu-central-1** — verify this exactly before clicking Create
       - After creation: Settings → Infrastructure → confirm region shows "eu-central-1"
       - Copy: Project URL, anon key, service_role key from Settings → API

    3. **Verify Clerk session token customization:**
       - Clerk Dashboard → Configure → Sessions → Customize session token
       - Add: `{ "metadata": "{{user.public_metadata}}" }` to the token claims
       - Save. This is required for superadmin role checks in middleware without extra API calls.

    Fill in `.env.local` with these values once provisioned. If either service is missing, STOP — the Wave 2+ plans cannot run without real keys.
  </how-to-verify>
  <resume-signal>Type "provisioned" after both Clerk and Supabase (Frankfurt) are set up and .env.local is filled in, OR type "skip" to use placeholder values for local dev (schema push in Plan 02 will fail without real Supabase credentials).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create Next.js 15.2.4 Project + Install Dependencies</name>
  <read_first>
    - C:/Users/Gaming/Desktop/AnamnezAl/.planning/phases/01-temel-altyapi/01-RESEARCH.md (§Standard Stack, §Installation commands)
    - C:/Users/Gaming/Desktop/AnamnezAl/.planning/phases/01-temel-altyapi/01-PATTERNS.md (Greenfield Notice, environment variable conventions)
    - C:/Users/Gaming/Desktop/AnamnezAl/CLAUDE.md (tech stack table)
  </read_first>
  <files>
    package.json,
    src/app/layout.tsx,
    src/app/globals.css,
    components.json,
    vercel.json,
    .env.example,
    .env.local,
    vitest.config.ts,
    playwright.config.ts,
    src/lib/supabase/server.ts,
    src/lib/supabase/admin.ts,
    src/lib/clerk/roles.ts
  </files>
  <action>
    Work from the project root: C:/Users/Gaming/Desktop/AnamnezAl

    **Step 1 — Check if Next.js app already exists:**
    Check if `src/app/layout.tsx` exists. If the project has already been scaffolded (a prior attempt), skip the `create-next-app` command and go straight to Step 2.

    If NOT scaffolded yet, run in the PARENT directory (Desktop), then move into AnamnezAl:
    ```
    npx create-next-app@15.2.4 AnamnezAl --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
    ```
    Note: `create-next-app` creates the folder. If AnamnezAl already exists as a git repo, use `npx create-next-app@15.2.4 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` from inside the AnamnezAl directory instead.

    **Step 2 — Pin Next.js version exactly:**
    Edit `package.json` — change `"next": "^15.x.x"` or any version string to `"next": "15.2.4"` (no caret, no tilde). This is mandatory for CVE-2025-29927.

    **Step 3 — Install all Phase 1 dependencies:**
    ```
    npm install @clerk/nextjs@7.3.0
    npm install @supabase/supabase-js @supabase/ssr@0.10.2
    npm install zod svix
    npm install -D vitest @vitejs/plugin-react @vitest/ui
    npm install -D @playwright/test
    npm install next-themes
    ```

    **Step 4 — Initialize shadcn/ui:**
    ```
    npx shadcn@latest init
    ```
    When prompted:
    - Style: Default
    - Base color: Slate
    - CSS variables: Yes
    - Global CSS path: src/app/globals.css
    - Tailwind config: leave blank (Tailwind v4 — no config file)
    - Components alias: @/components/ui
    - Utils alias: @/lib/utils

    After init, add all required components:
    ```
    npx shadcn@latest add button input label form card dialog table badge sonner dropdown-menu alert separator
    ```

    **Step 5 — Update src/app/globals.css with UI-SPEC design tokens:**
    Replace the generated globals.css entirely with:
    ```css
    @import "tailwindcss";
    @import "tw-animate-css";

    @custom-variant dark (&:is(.dark *));

    @theme inline {
      --color-background: var(--background);
      --color-foreground: var(--foreground);
      --font-sans: var(--font-inter);
      --radius-lg: var(--radius);
      --radius-md: calc(var(--radius) - 2px);
      --radius-sm: calc(var(--radius) - 4px);
    }

    :root {
      /* UI-SPEC colors */
      --background: #F8FAFC;           /* slate-50 — dominant 60% */
      --foreground: #0F172A;           /* slate-900 — primary text */
      --card: #FFFFFF;
      --card-foreground: #0F172A;
      --popover: #FFFFFF;
      --popover-foreground: #0F172A;
      --primary: #2563EB;              /* blue-600 — accent 10% */
      --primary-foreground: #FFFFFF;
      --secondary: #F1F5F9;            /* slate-100 */
      --secondary-foreground: #0F172A;
      --muted: #F1F5F9;
      --muted-foreground: #475569;     /* slate-600 — secondary text */
      --accent: #2563EB;
      --accent-foreground: #FFFFFF;
      --destructive: #DC2626;          /* red-600 */
      --destructive-foreground: #FFFFFF;
      --border: #E2E8F0;               /* slate-200 */
      --input: #E2E8F0;
      --ring: #2563EB;
      --radius: 0.75rem;              /* 12px — card border-radius from UI-SPEC */
      /* Status colors */
      --success: #16A34A;             /* green-600 */
      --warning: #D97706;             /* amber-600 */
      --neutral-badge: #64748B;       /* slate-500 */
    }
    ```

    **Step 6 — Update src/app/layout.tsx:**
    Replace with the ClerkProvider + Inter font pattern from PATTERNS.md. Add `lang="tr"` to `<html>`.

    ```typescript
    import { ClerkProvider } from '@clerk/nextjs'
    import { Inter } from 'next/font/google'
    import type { Metadata } from 'next'
    import './globals.css'

    const inter = Inter({
      subsets: ['latin'],
      variable: '--font-inter',
    })

    export const metadata: Metadata = {
      title: 'AnamnezAl',
      description: 'Diş hekimi için sesli anamnez uygulaması',
    }

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <ClerkProvider>
          <html lang="tr" className={inter.variable}>
            <body className="bg-background text-foreground antialiased">{children}</body>
          </html>
        </ClerkProvider>
      )
    }
    ```

    **Step 7 — Create vercel.json:**
    ```json
    {
      "regions": ["fra1"],
      "functions": {
        "app/api/**": {
          "maxDuration": 30
        }
      }
    }
    ```

    **Step 8 — Create .env.example:**
    ```
    # Clerk
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
    CLERK_SECRET_KEY=sk_test_...
    CLERK_WEBHOOK_SECRET=whsec_...

    # Supabase (project MUST be in eu-central-1 Frankfurt for KVKK compliance)
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
    SUPABASE_SERVICE_ROLE_KEY=eyJ...

    # App
    NEXT_PUBLIC_ROOT_DOMAIN=localhost
    # Production: NEXT_PUBLIC_ROOT_DOMAIN=anamnezal.com
    ```

    **Step 9 — Create .env.local** (if not already present from Task 1 checkpoint):
    Copy .env.example to .env.local and fill in real values from Clerk and Supabase dashboards. If Task 1 was skipped, use placeholder values — schema push in Plan 02 will require real values.

    **Step 10 — Create vitest.config.ts:**
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import { resolve } from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: ['./src/lib/__tests__/setup.ts'],
      },
      resolve: {
        alias: {
          '@': resolve(__dirname, './src'),
        },
      },
    })
    ```

    **Step 11 — Create playwright.config.ts:**
    ```typescript
    import { defineConfig, devices } from '@playwright/test'

    export default defineConfig({
      testDir: './e2e',
      fullyParallel: true,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 0,
      use: {
        baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000',
        trace: 'on-first-retry',
      },
      projects: [
        { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
      ],
    })
    ```

    **Step 12 — Create lib utility files from PATTERNS.md:**

    Create `src/lib/supabase/server.ts`:
    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import { auth } from '@clerk/nextjs/server'

    export async function createSupabaseServerClient() {
      const { getToken } = await auth()

      return createClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          global: {
            fetch: async (url, options = {}) => {
              const clerkToken = await getToken()
              const headers = new Headers(options?.headers)
              headers.set('Authorization', `Bearer ${clerkToken}`)
              return fetch(url, { ...options, headers })
            },
          },
        }
      )
    }
    ```

    Create `src/lib/supabase/admin.ts`:
    ```typescript
    import { createClient } from '@supabase/supabase-js'

    // NEVER import this file from client components or files with 'use client'
    // ONLY use in: Server Components, Route Handlers, Server Actions
    export const supabaseAdmin = createClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.SUPABASE_SERVICE_ROLE_KEY!,
      {
        auth: {
          autoRefreshToken: false,
          persistSession: false,
        },
      }
    )
    ```

    Create `src/lib/clerk/roles.ts`:
    ```typescript
    import { auth } from '@clerk/nextjs/server'

    export type AppRole = 'superadmin' | 'org:admin' | 'org:dentist' | 'org:assistant'

    export async function getRole(): Promise<AppRole | null> {
      const { sessionClaims, orgRole } = await auth()

      if (sessionClaims?.metadata?.role === 'superadmin') {
        return 'superadmin'
      }

      if (orgRole) {
        return orgRole as AppRole
      }

      return null
    }

    export async function requireSuperadmin() {
      const role = await getRole()
      if (role !== 'superadmin') {
        throw new Error('Unauthorized: superadmin required')
      }
    }

    export async function requireOrgRole(allowedRoles: AppRole[]) {
      const role = await getRole()
      if (!role || !allowedRoles.includes(role)) {
        throw new Error('Unauthorized: insufficient role')
      }
    }
    ```

    Create `src/lib/__tests__/setup.ts` (Vitest global setup):
    ```typescript
    // Global test setup
    // Add test environment config here as needed
    ```

    Create `e2e/.gitkeep` (placeholder for Playwright tests):
    ```
    ```
  </action>
  <verify>
    <automated>
      1. `node -e "const p = require('./package.json'); console.assert(p.dependencies.next === '15.2.4', 'next version wrong'); console.log('next version OK:', p.dependencies.next)"` — must print "next version OK: 15.2.4"
      2. `node -e "require('./components.json')"` — must not throw (valid JSON)
      3. Check vercel.json: `node -e "const v = require('./vercel.json'); console.assert(v.regions.includes('fra1'), 'fra1 missing'); console.log('fra1 OK')"` — must print "fra1 OK"
      4. `npx tsc --noEmit` — must succeed (zero TypeScript errors)
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"next": "15.2.4"` (exact string, no caret or tilde)
    - `package.json` contains `"@clerk/nextjs": "7.3.0"`
    - `package.json` contains `"@supabase/ssr"` and `"@supabase/supabase-js"`
    - `components.json` exists and contains `"css": "src/app/globals.css"`
    - `components.json` contains `"config": ""` (Tailwind v4 — no config file path)
    - `vercel.json` contains `"fra1"` in the regions array
    - `src/app/globals.css` contains `@theme` block
    - `src/app/globals.css` contains `--primary: #2563EB`
    - `src/app/layout.tsx` contains `ClerkProvider`
    - `src/app/layout.tsx` contains `lang="tr"`
    - `src/lib/supabase/server.ts` does NOT contain `template: 'supabase'`
    - `src/lib/supabase/admin.ts` does NOT contain `NEXT_PUBLIC_`
    - `src/lib/clerk/roles.ts` exports `AppRole`, `getRole`, `requireSuperadmin`, `requireOrgRole`
    - `vitest.config.ts` exists
    - `playwright.config.ts` exists
    - `.env.example` documents all 7 required environment variables
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>
    Next.js 15.2.4 project is scaffolded, shadcn/ui initialized with Tailwind v4 tokens, vercel.json pins fra1, all lib utilities created, test infrastructure configured. Zero TypeScript errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Next.js Edge | Public internet requests hit middleware before reaching app routes |
| Environment → Build | Build process must not bundle server-only secrets into client JS |
| Dependency supply chain | npm packages could inject malicious code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `package.json` next version | mitigate | Pin `"next": "15.2.4"` exactly — no caret/tilde. CVE-2025-29927 is exploitable on <15.2.3. Lock file committed. |
| T-01-02 | Information Disclosure | `src/lib/supabase/admin.ts` | mitigate | `SUPABASE_SERVICE_ROLE_KEY` has no `NEXT_PUBLIC_` prefix. File only importable in server context. Accept CI check in Plan 03 to enforce. |
| T-01-03 | Information Disclosure | `.env.local` | accept | `.env.local` is git-ignored by Next.js default. `.env.example` committed with placeholder values only — no real secrets. |
| T-01-04 | Tampering | `vercel.json` regions config | mitigate | `fra1` must be committed in version control. KVKK data residency depends on it. Verified in acceptance criteria. |
| T-01-05 | Elevation of Privilege | Clerk session token customization | mitigate | `publicMetadata` must be added to session token in Clerk Dashboard. Without it, superadmin role check silently fails (returns undefined instead of 'superadmin'). Documented in Task 1 checkpoint. |
</threat_model>

<verification>
After completing both tasks:
1. `npm run build` succeeds (no build errors)
2. `npm run dev` starts without errors
3. `cat package.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); process.exit(d.dependencies.next==='15.2.4'?0:1)"` exits 0
4. `cat vercel.json | node -e "const v=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); process.exit(v.regions.includes('fra1')?0:1)"` exits 0
5. `cat components.json` shows shadcn config with `"config": ""`
</verification>

<success_criteria>
- Next.js 15.2.4 application boots with `npm run dev`
- shadcn/ui initialized — components.json committed with Tailwind v4 settings
- All Phase 1 shadcn components installed (button, input, label, form, card, dialog, table, badge, sonner, dropdown-menu, alert, separator)
- Tailwind v4 @theme block in globals.css with UI-SPEC color tokens
- vercel.json committed with `"regions": ["fra1"]`
- .env.example documents all required environment variables
- src/lib/supabase/server.ts, admin.ts, and src/lib/clerk/roles.ts created with correct patterns
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-temel-altyapi/01-01-SUMMARY.md` with:
- Files created/modified
- Dependencies installed and their versions
- Any deviations from plan (e.g., shadcn init prompts answered differently)
- State of .env.local (filled with real values vs placeholders)
</output>
