---
phase: 05-dental-ai-aciklamalari
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/sessions/DescriptionPopover.tsx
  - src/components/sessions/AnamnesisForm.tsx
autonomous: true
requirements: [DESC-01, DESC-05, DESC-06]
must_haves:
  truths:
    - "Medication/disease/allergy answers show a collapsed click-to-expand button, non-intrusive"
    - "Clicking expands the 3-line dental description fetched from /api/descriptions"
    - "The expanded panel shows the legal disclaimer returned by the server"
    - "Non-medical answers show no description button"
  artifacts:
    - path: "src/components/sessions/DescriptionPopover.tsx"
      provides: "collapsed-by-default expandable description trigger + 3-line panel + disclaimer"
      min_lines: 60
    - path: "src/components/sessions/AnamnesisForm.tsx"
      provides: "per-term description buttons wired via classifyAnswer + extractTerms"
      contains: "DescriptionPopover"
  key_links:
    - from: "src/components/sessions/AnamnesisForm.tsx"
      to: "src/lib/descriptions/classifier.ts"
      via: "classifyAnswer + extractTerms per rendered answer"
      pattern: "classifyAnswer|extractTerms"
    - from: "src/components/sessions/DescriptionPopover.tsx"
      to: "/api/descriptions"
      via: "fetch on first expand"
      pattern: "fetch.*api/descriptions"
---

<objective>
The UI surface: a collapsed-by-default expandable description control that fetches the 3-line dental description on first open and renders it with the server-supplied disclaimer, wired into AnamnesisForm for every medication/disease/allergy term.

Purpose: DESC-01 (click-to-expand per term, non-intrusive) + DESC-05 (optional, default collapsed) + DESC-06 (disclaimer visible). Closes Phase 5.
Output: 1 new client component + AnamnesisForm modified to attach per-term buttons. No new API surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dental-ai-aciklamalari/05-CONTEXT.md
@.planning/phases/05-dental-ai-aciklamalari/05-02-SUMMARY.md

<interfaces>
<!-- From src/lib/descriptions/classifier.ts + types.ts (05-01) -->
```typescript
export type DescriptionCategory = 'medication' | 'disease' | 'allergy'
export function classifyAnswer(a: { prompt: string; question_type: QuestionType; answer_value: AnswerValue }): DescriptionCategory | null
export function extractTerms(value: AnswerValue): string[]
export interface DentalDescription {
  display_term: string; category: DescriptionCategory
  dental_impact: string; risk_level: string; precaution: string
  active_ingredient: string | null; disclaimer: string
}
```

<!-- POST /api/descriptions (05-02): body { term, category } → 200 DentalDescription | 502 { error } -->

<!-- AnamnesisForm current state (Phase 4) — the per-question render block to extend:
     each question renders inside `<div className="space-y-2 rounded-lg border ...">`;
     label + ConfidenceBadge in a flex header, then renderInput(q).
     `fields` is Record<questionId, { answer_value, confidence, edited_by_human }>.
     `orderedQuestions: SnapshotQuestion[]` (id, prompt, question_type, options, position, required).
     Existing imports: Button, Badge, Input from @/components/ui/*; lucide-react icons; sonner toast. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DescriptionPopover component (collapsed-by-default expand)</name>
  <read_first>
    - src/components/sessions/AnamnesisForm.tsx (Badge/Button usage, Loader2 spinner pattern, toast usage to mirror)
    - src/lib/descriptions/types.ts (DentalDescription, DescriptionCategory)
  </read_first>
  <files>src/components/sessions/DescriptionPopover.tsx</files>
  <action>
Create `'use client'` component `DescriptionPopover`.

Props: `{ term: string; category: DescriptionCategory }`.

Behavior (DESC-05: default collapsed; DESC-01: non-intrusive click-to-expand):
- Local state: `open` (default false), `loading`, `data: DentalDescription | null`, `error: string | null`.
- Render a small inline trigger button (use existing `Button` `variant="ghost" size="sm"` + a lucide `Info` or `ChevronDown` icon) labeled by the term, e.g. `{term}` with a tiny "açıklama" hint — compact, sits next to the answer value, not a full-width block (non-intrusive).
- On first click that opens it: if `data` is null and not loading, `fetch('/api/descriptions', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ term, category }) })`. On !res.ok → read `{ error }`, set error state + `toast.error`. On success → set `data`. Cache in component state so re-opening does not refetch.
- When `open` and `loading` → show `<Loader2 className="h-4 w-4 animate-spin" />` + "Açıklama yükleniyor…".
- When `open` and `data` → render a bordered panel (mirror AnamnesisForm's `rounded-lg border border-border bg-card p-3` style) with EXACTLY 3 labeled lines:
  - "Diş/cerrahi/anestezi etkisi: {data.dental_impact}"
  - "Risk düzeyi: {data.risk_level}"
  - "Önerilen önlem: {data.precaution}"
  - then the disclaimer in muted small text: `{data.disclaimer}` (DESC-06 — render whatever the server returned; do not hardcode it client-side).
- When `open` and `error` → show the error text + a "Tekrar dene" button that clears error and refetches.
- Toggling closed hides the panel but keeps `data` (so reopening is instant).

Keep it ~60-110 lines. No new deps — reuse Button + lucide-react (already imported elsewhere) + sonner toast.
  </action>
  <verify>
    <automated>grep -q "'use client'" src/components/sessions/DescriptionPopover.tsx && grep -q "api/descriptions" src/components/sessions/DescriptionPopover.tsx && grep -q "data.dental_impact" src/components/sessions/DescriptionPopover.tsx && grep -q "data.risk_level" src/components/sessions/DescriptionPopover.tsx && grep -q "data.precaution" src/components/sessions/DescriptionPopover.tsx && grep -q "data.disclaimer" src/components/sessions/DescriptionPopover.tsx && npx tsc --noEmit -p . && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Component is `'use client'`, default collapsed (open initial state false).
    - Fetches POST /api/descriptions only on first expand; result cached in state (no refetch on reopen).
    - Renders exactly the 3 lines (dental_impact, risk_level, precaution) + the server `disclaimer` (not a client literal).
    - Loading spinner + error/retry handled.
    - `npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>Collapsed-by-default expandable popover fetches and renders the 3-line description + server disclaimer.</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-term description buttons into AnamnesisForm</name>
  <read_first>
    - src/components/sessions/AnamnesisForm.tsx (the full file — per-question render block + fields state)
    - src/lib/descriptions/classifier.ts (classifyAnswer, extractTerms)
    - src/components/sessions/DescriptionPopover.tsx (props { term, category } — created Task 1)
  </read_first>
  <files>src/components/sessions/AnamnesisForm.tsx</files>
  <action>
Modify AnamnesisForm to attach DescriptionPopover buttons (DESC-01).

1. Add imports: `import DescriptionPopover from './DescriptionPopover'` and `import { classifyAnswer, extractTerms } from '@/lib/descriptions/classifier'`.
2. Inside the `orderedQuestions.map((q) => ...)` render block, AFTER `renderInput(q)`, compute the describable terms for this answer:
```tsx
const field = fields[q.id]
const category = classifyAnswer({
  prompt: q.prompt,
  question_type: q.question_type,
  answer_value: field?.answer_value ?? null,
})
const terms = category ? extractTerms(field?.answer_value ?? null) : []
```
   Render a description row only when `category && terms.length > 0`:
```tsx
{category && terms.length > 0 && (
  <div className="flex flex-wrap items-center gap-2 pt-1">
    <span className="text-xs text-muted-foreground">Açıklamalar:</span>
    {terms.map((t) => (
      <DescriptionPopover key={t} term={t} category={category} />
    ))}
  </div>
)}
```
   Because each popover manages its own open/close, multiple terms in one answer (e.g. "Parol, aspirin") each get their own collapsed button (DESC-01 per-term).
3. Recompute `category`/`terms` reactively — they derive from `fields[q.id].answer_value`, which is already React state, so edits that change the answer update the buttons automatically. Do NOT memoize across the whole list; compute inline per question inside the map (cheap).
4. Non-medical answers (`category === null`) render no row → non-intrusive (DESC-05).

Do not change any Phase 4 behavior (AI fill, confidence badges, missing alerts, consent gate). Only ADD the description row inside each question card.
  </action>
  <verify>
    <automated>grep -q "import DescriptionPopover" src/components/sessions/AnamnesisForm.tsx && grep -q "classifyAnswer" src/components/sessions/AnamnesisForm.tsx && grep -q "extractTerms" src/components/sessions/AnamnesisForm.tsx && grep -q "<DescriptionPopover" src/components/sessions/AnamnesisForm.tsx && grep -q "ConsentGate" src/components/sessions/AnamnesisForm.tsx && npx tsc --noEmit -p . && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - AnamnesisForm imports DescriptionPopover + classifyAnswer + extractTerms.
    - Description row rendered only when classifyAnswer returns a category AND extractTerms is non-empty (medication/disease/allergy with a value).
    - One DescriptionPopover per extracted term (multi-term answers → multiple buttons).
    - All Phase 4 features still present (ConsentGate, confidence badges, missing alerts intact — grep ConsentGate passes; `npm test` shows no regression).
    - `npx tsc --noEmit -p .` exits 0; full `npm test` green.
  </acceptance_criteria>
  <done>Every medication/disease/allergy term in the filled form shows a collapsed description button; non-medical answers show none; Phase 4 behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fetch→/api/descriptions | client sends term+category; server (05-02) validates + scopes per user |
| rendered description text | server-supplied; disclaimer comes from server response, not client literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | client could hardcode/omit disclaimer | mitigate | popover renders `data.disclaimer` from the server response — no client-side disclaimer literal; server (05-02) always appends it |
| T-05-10 | Information Disclosure | description for another user's term | accept | descriptions are non-PII generic clinical text; cache is still per-user server-side (05-02). No patient identifier crosses this boundary |
</threat_model>

<verification>
- `npm test` full suite green (no Phase 4 regression).
- `npx tsc --noEmit -p .` exits 0.
- AnamnesisForm renders a DescriptionPopover per medication/disease/allergy term; non-medical answers render none.
- Popover collapsed by default; expands to 3 lines + server disclaimer.
</verification>

<success_criteria>
- Each medication, disease, allergy term in the filled form has a click-to-expand button, collapsed by default, non-intrusive (DESC-01, DESC-05).
- Expanded panel shows exactly 3 lines + the legal disclaimer returned by the server (DESC-06).
- Phase 4 anamnesis flow (AI fill, edits, consent gate) unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dental-ai-aciklamalari/05-03-SUMMARY.md`
</output>
